---
phase: 06-menu-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/src/test/java/com/mudita/sudoku/ui/game/MenuScreenTest.kt
  - app/src/test/java/com/mudita/sudoku/ui/game/DifficultyScreenTest.kt
  - app/src/test/java/com/mudita/sudoku/ui/game/SummaryScreenTest.kt
  - app/src/test/java/com/mudita/sudoku/ui/game/LeaderboardScreenTest.kt
autonomous: true
requirements:
  - NAV-01

must_haves:
  truths:
    - "MenuScreen tests verify title display, button presence, conditional Resume, and callback invocation"
    - "DifficultyScreen tests verify heading display, all three difficulty buttons, callback invocation, and Back button"
    - "SummaryScreenTest uses onBackToMenu and asserts 'Back to Menu' button text"
    - "LeaderboardScreenTest uses onBackToMenu and asserts 'Back to Menu' button text"
  artifacts:
    - path: "app/src/test/java/com/mudita/sudoku/ui/game/MenuScreenTest.kt"
      provides: "Robolectric UI tests for MenuScreen"
      contains: "class MenuScreenTest"
    - path: "app/src/test/java/com/mudita/sudoku/ui/game/DifficultyScreenTest.kt"
      provides: "Robolectric UI tests for DifficultyScreen"
      contains: "class DifficultyScreenTest"
  key_links:
    - from: "app/src/test/java/com/mudita/sudoku/ui/game/MenuScreenTest.kt"
      to: "MenuScreen composable"
      via: "createComposeRule setContent"
      pattern: "MenuScreen\\("
    - from: "app/src/test/java/com/mudita/sudoku/ui/game/DifficultyScreenTest.kt"
      to: "DifficultyScreen composable"
      via: "createComposeRule setContent"
      pattern: "DifficultyScreen\\("
---

<objective>
Create Robolectric Compose UI tests for MenuScreen and DifficultyScreen, and update existing SummaryScreenTest and LeaderboardScreenTest to match the renamed callbacks and button labels.

Purpose: Automated verification of all NAV-01 composable behaviors — menu display, conditional resume, difficulty selection, and updated navigation labels.
Output: Four test files (2 new, 2 modified) covering NAV-01a through NAV-01f from the validation strategy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-menu-navigation/06-CONTEXT.md
@.planning/phases/06-menu-navigation/06-VALIDATION.md
@.planning/phases/06-menu-navigation/06-01-SUMMARY.md

<interfaces>
<!-- Composable signatures from Plan 01 output — executor verifies against actual files -->

From app/src/main/java/com/mudita/sudoku/ui/game/MenuScreen.kt (created in Plan 01):
```kotlin
@Composable
fun MenuScreen(
    hasSavedGame: Boolean,
    onNewGame: () -> Unit,
    onResume: () -> Unit,
    onBestScores: () -> Unit
)
// Contains: TextMMD("Sudoku"), TextMMD("New Game"), TextMMD("Resume") (conditional), TextMMD("Best Scores")
```

From app/src/main/java/com/mudita/sudoku/ui/game/DifficultyScreen.kt (created in Plan 01):
```kotlin
@Composable
fun DifficultyScreen(
    onDifficultySelected: (Difficulty) -> Unit,
    onBack: () -> Unit
)
// Contains: TextMMD("Select Difficulty"), TextMMD("Easy"), TextMMD("Medium"), TextMMD("Hard"), TextMMD("Back")
```

From app/src/main/java/com/mudita/sudoku/ui/game/SummaryScreen.kt (modified in Plan 01):
```kotlin
@Composable
fun SummaryScreen(
    result: CompletionResult,
    onViewLeaderboard: () -> Unit,
    onBackToMenu: () -> Unit    // was onNewGame
)
// Button label: "Back to Menu" (was "New Game")
```

From app/src/main/java/com/mudita/sudoku/ui/game/LeaderboardScreen.kt (modified in Plan 01):
```kotlin
@Composable
fun LeaderboardScreen(
    scores: Map<Difficulty, Int?>,
    onBackToMenu: () -> Unit    // was onNewGame
)
// Button label: "Back to Menu" (was "New Game")
```

Existing test patterns from app/src/test/java/com/mudita/sudoku/ui/game/SummaryScreenTest.kt:
```kotlin
@RunWith(RobolectricTestRunner::class)
@Config(sdk = [31])
class SummaryScreenTest {
    @get:Rule val composeRule = createComposeRule()
    // Pattern: setContent { ThemeMMD { ScreenComposable(...) } }
    // Assert: composeRule.onNodeWithText("...").assertIsDisplayed()
    // Callback: var callCount = 0; onClick = { callCount++ }; performClick(); assertEquals(1, callCount)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MenuScreenTest and DifficultyScreenTest</name>
  <files>
    app/src/test/java/com/mudita/sudoku/ui/game/MenuScreenTest.kt,
    app/src/test/java/com/mudita/sudoku/ui/game/DifficultyScreenTest.kt
  </files>
  <read_first>
    app/src/test/java/com/mudita/sudoku/ui/game/SummaryScreenTest.kt,
    app/src/test/java/com/mudita/sudoku/ui/game/LeaderboardScreenTest.kt,
    app/src/main/java/com/mudita/sudoku/ui/game/MenuScreen.kt,
    app/src/main/java/com/mudita/sudoku/ui/game/DifficultyScreen.kt
  </read_first>
  <action>
Create two new test files following the exact pattern from `SummaryScreenTest.kt` and `LeaderboardScreenTest.kt` — same annotations, same rule setup, same ThemeMMD wrapping.

**MenuScreenTest.kt** — covers NAV-01a and NAV-01b:

```kotlin
package com.mudita.sudoku.ui.game

import androidx.compose.ui.test.assertIsDisplayed
import androidx.compose.ui.test.assertIsNotDisplayed
import androidx.compose.ui.test.junit4.createComposeRule
import androidx.compose.ui.test.onNodeWithText
import androidx.compose.ui.test.performClick
import com.mudita.mmd.ThemeMMD
import org.junit.Assert.assertEquals
import org.junit.Rule
import org.junit.Test
import org.junit.runner.RunWith
import org.robolectric.RobolectricTestRunner
import org.robolectric.annotation.Config

@RunWith(RobolectricTestRunner::class)
@Config(sdk = [31])
class MenuScreenTest {

    @get:Rule
    val composeRule = createComposeRule()

    // --- title

    @Test
    fun `displays Sudoku title`() {
        composeRule.setContent {
            ThemeMMD {
                MenuScreen(hasSavedGame = false, onNewGame = {}, onResume = {}, onBestScores = {})
            }
        }
        composeRule.onNodeWithText("Sudoku").assertIsDisplayed()
    }

    // --- buttons always visible

    @Test
    fun `displays New Game button`() {
        composeRule.setContent {
            ThemeMMD {
                MenuScreen(hasSavedGame = false, onNewGame = {}, onResume = {}, onBestScores = {})
            }
        }
        composeRule.onNodeWithText("New Game").assertIsDisplayed()
    }

    @Test
    fun `displays Best Scores button`() {
        composeRule.setContent {
            ThemeMMD {
                MenuScreen(hasSavedGame = false, onNewGame = {}, onResume = {}, onBestScores = {})
            }
        }
        composeRule.onNodeWithText("Best Scores").assertIsDisplayed()
    }

    // --- conditional Resume button

    @Test
    fun `shows Resume button when hasSavedGame is true`() {
        composeRule.setContent {
            ThemeMMD {
                MenuScreen(hasSavedGame = true, onNewGame = {}, onResume = {}, onBestScores = {})
            }
        }
        composeRule.onNodeWithText("Resume").assertIsDisplayed()
    }

    @Test
    fun `hides Resume button when hasSavedGame is false`() {
        composeRule.setContent {
            ThemeMMD {
                MenuScreen(hasSavedGame = false, onNewGame = {}, onResume = {}, onBestScores = {})
            }
        }
        composeRule.onNodeWithText("Resume").assertDoesNotExist()
    }

    // --- callbacks

    @Test
    fun `New Game button calls onNewGame callback`() {
        var callCount = 0
        composeRule.setContent {
            ThemeMMD {
                MenuScreen(hasSavedGame = false, onNewGame = { callCount++ }, onResume = {}, onBestScores = {})
            }
        }
        composeRule.onNodeWithText("New Game").performClick()
        assertEquals(1, callCount)
    }

    @Test
    fun `Resume button calls onResume callback`() {
        var callCount = 0
        composeRule.setContent {
            ThemeMMD {
                MenuScreen(hasSavedGame = true, onNewGame = {}, onResume = { callCount++ }, onBestScores = {})
            }
        }
        composeRule.onNodeWithText("Resume").performClick()
        assertEquals(1, callCount)
    }

    @Test
    fun `Best Scores button calls onBestScores callback`() {
        var callCount = 0
        composeRule.setContent {
            ThemeMMD {
                MenuScreen(hasSavedGame = false, onNewGame = {}, onResume = {}, onBestScores = { callCount++ })
            }
        }
        composeRule.onNodeWithText("Best Scores").performClick()
        assertEquals(1, callCount)
    }
}
```

**DifficultyScreenTest.kt** — covers NAV-01c and NAV-01d:

```kotlin
package com.mudita.sudoku.ui.game

import androidx.compose.ui.test.assertIsDisplayed
import androidx.compose.ui.test.junit4.createComposeRule
import androidx.compose.ui.test.onNodeWithText
import androidx.compose.ui.test.performClick
import com.mudita.mmd.ThemeMMD
import com.mudita.sudoku.puzzle.model.Difficulty
import org.junit.Assert.assertEquals
import org.junit.Rule
import org.junit.Test
import org.junit.runner.RunWith
import org.robolectric.RobolectricTestRunner
import org.robolectric.annotation.Config

@RunWith(RobolectricTestRunner::class)
@Config(sdk = [31])
class DifficultyScreenTest {

    @get:Rule
    val composeRule = createComposeRule()

    // --- heading

    @Test
    fun `displays Select Difficulty heading`() {
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = {}, onBack = {})
            }
        }
        composeRule.onNodeWithText("Select Difficulty").assertIsDisplayed()
    }

    // --- difficulty buttons

    @Test
    fun `displays Easy button`() {
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = {}, onBack = {})
            }
        }
        composeRule.onNodeWithText("Easy").assertIsDisplayed()
    }

    @Test
    fun `displays Medium button`() {
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = {}, onBack = {})
            }
        }
        composeRule.onNodeWithText("Medium").assertIsDisplayed()
    }

    @Test
    fun `displays Hard button`() {
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = {}, onBack = {})
            }
        }
        composeRule.onNodeWithText("Hard").assertIsDisplayed()
    }

    @Test
    fun `displays Back button`() {
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = {}, onBack = {})
            }
        }
        composeRule.onNodeWithText("Back").assertIsDisplayed()
    }

    // --- callbacks

    @Test
    fun `Easy button calls onDifficultySelected with EASY`() {
        var selected: Difficulty? = null
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = { selected = it }, onBack = {})
            }
        }
        composeRule.onNodeWithText("Easy").performClick()
        assertEquals(Difficulty.EASY, selected)
    }

    @Test
    fun `Medium button calls onDifficultySelected with MEDIUM`() {
        var selected: Difficulty? = null
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = { selected = it }, onBack = {})
            }
        }
        composeRule.onNodeWithText("Medium").performClick()
        assertEquals(Difficulty.MEDIUM, selected)
    }

    @Test
    fun `Hard button calls onDifficultySelected with HARD`() {
        var selected: Difficulty? = null
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = { selected = it }, onBack = {})
            }
        }
        composeRule.onNodeWithText("Hard").performClick()
        assertEquals(Difficulty.HARD, selected)
    }

    @Test
    fun `Back button calls onBack callback`() {
        var callCount = 0
        composeRule.setContent {
            ThemeMMD {
                DifficultyScreen(onDifficultySelected = {}, onBack = { callCount++ })
            }
        }
        composeRule.onNodeWithText("Back").performClick()
        assertEquals(1, callCount)
    }
}
```
  </action>
  <verify>
    <automated>cd D:/Development/KompaktSudoku && ./gradlew :app:testDebugUnitTest --tests "*.MenuScreenTest" --tests "*.DifficultyScreenTest" -x lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - MenuScreenTest.kt exists at app/src/test/java/com/mudita/sudoku/ui/game/MenuScreenTest.kt
    - MenuScreenTest.kt contains `class MenuScreenTest`
    - MenuScreenTest.kt contains `onNodeWithText("Sudoku").assertIsDisplayed()`
    - MenuScreenTest.kt contains `onNodeWithText("Resume").assertDoesNotExist()`
    - MenuScreenTest.kt contains `onNodeWithText("Resume").assertIsDisplayed()`
    - MenuScreenTest.kt contains at least 8 `@Test` annotations
    - DifficultyScreenTest.kt exists at app/src/test/java/com/mudita/sudoku/ui/game/DifficultyScreenTest.kt
    - DifficultyScreenTest.kt contains `class DifficultyScreenTest`
    - DifficultyScreenTest.kt contains `Difficulty.EASY`
    - DifficultyScreenTest.kt contains `Difficulty.MEDIUM`
    - DifficultyScreenTest.kt contains `Difficulty.HARD`
    - DifficultyScreenTest.kt contains at least 8 `@Test` annotations
    - All tests in MenuScreenTest pass
    - All tests in DifficultyScreenTest pass
  </acceptance_criteria>
  <done>MenuScreenTest covers title, buttons, conditional Resume, and all callbacks. DifficultyScreenTest covers heading, all difficulty buttons, Back button, and all callbacks with correct Difficulty enum values.</done>
</task>

<task type="auto">
  <name>Task 2: Update SummaryScreenTest and LeaderboardScreenTest for renamed callbacks</name>
  <files>
    app/src/test/java/com/mudita/sudoku/ui/game/SummaryScreenTest.kt,
    app/src/test/java/com/mudita/sudoku/ui/game/LeaderboardScreenTest.kt
  </files>
  <read_first>
    app/src/test/java/com/mudita/sudoku/ui/game/SummaryScreenTest.kt,
    app/src/test/java/com/mudita/sudoku/ui/game/LeaderboardScreenTest.kt,
    app/src/main/java/com/mudita/sudoku/ui/game/SummaryScreen.kt,
    app/src/main/java/com/mudita/sudoku/ui/game/LeaderboardScreen.kt
  </read_first>
  <action>
Update both existing test files to match the renamed callback parameters and button labels from Plan 01.

**SummaryScreenTest.kt changes:**

1. In ALL test methods that call `SummaryScreen(...)`, rename `onNewGame = ...` to `onBackToMenu = ...`. There are 8 test methods — every call site must be updated. Specific occurrences:
   - `displays Puzzle Complete heading` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `displays difficulty label in title case` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `displays error count` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `displays hint count` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `displays final score` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `shows personal best notice` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `hides personal best notice` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `View Leaderboard button` test: `onNewGame = {}` -> `onBackToMenu = {}`

2. Rename the last test from `New Game button calls onNewGame callback` to `Back to Menu button calls onBackToMenu callback`. Update:
   - Button text assertion from `"New Game"` to `"Back to Menu"`: `composeRule.onNodeWithText("Back to Menu").performClick()`
   - Callback parameter from `onNewGame = { callCount++ }` to `onBackToMenu = { callCount++ }`
   - Assertion message from `"New Game click should invoke callback once"` to `"Back to Menu click should invoke callback once"`

**LeaderboardScreenTest.kt changes:**

1. In ALL test methods that call `LeaderboardScreen(...)`, rename `onNewGame = ...` to `onBackToMenu = ...`. There are 5 test methods:
   - `displays Best Scores heading` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `displays stored score for Easy` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `displays em-dash for difficulty with no score` test: `onNewGame = {}` -> `onBackToMenu = {}`
   - `displays all three difficulty labels` test: `onNewGame = {}` -> `onBackToMenu = {}`

2. Rename the last test from `New Game button calls onNewGame callback` to `Back to Menu button calls onBackToMenu callback`. Update:
   - Button text assertion from `"New Game"` to `"Back to Menu"`: `composeRule.onNodeWithText("Back to Menu").performClick()`
   - Callback parameter from `onNewGame = { callCount++ }` to `onBackToMenu = { callCount++ }`
   - Assertion message from `"New Game click should invoke callback once"` to `"Back to Menu click should invoke callback once"`
  </action>
  <verify>
    <automated>cd D:/Development/KompaktSudoku && ./gradlew :app:testDebugUnitTest --tests "*.SummaryScreenTest" --tests "*.LeaderboardScreenTest" -x lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SummaryScreenTest.kt does NOT contain `onNewGame`
    - SummaryScreenTest.kt contains `onBackToMenu` in every SummaryScreen call
    - SummaryScreenTest.kt contains `onNodeWithText("Back to Menu").performClick()`
    - SummaryScreenTest.kt does NOT contain `onNodeWithText("New Game")`
    - LeaderboardScreenTest.kt does NOT contain `onNewGame`
    - LeaderboardScreenTest.kt contains `onBackToMenu` in every LeaderboardScreen call
    - LeaderboardScreenTest.kt contains `onNodeWithText("Back to Menu").performClick()`
    - LeaderboardScreenTest.kt does NOT contain `onNodeWithText("New Game")`
    - All tests in SummaryScreenTest pass
    - All tests in LeaderboardScreenTest pass
  </acceptance_criteria>
  <done>SummaryScreenTest and LeaderboardScreenTest use onBackToMenu callbacks and assert "Back to Menu" button text. All existing tests pass with the renamed parameters.</done>
</task>

</tasks>

<verification>
1. `./gradlew :app:testDebugUnitTest -x lint` — full test suite passes (all 4 test files + all prior tests)
2. MenuScreenTest: 8+ tests green (title, buttons, conditional resume, callbacks)
3. DifficultyScreenTest: 8+ tests green (heading, difficulty buttons, back, callbacks)
4. SummaryScreenTest: 8 tests green (no references to "New Game" or onNewGame)
5. LeaderboardScreenTest: 5 tests green (no references to "New Game" or onNewGame)
</verification>

<success_criteria>
- All 4 test files compile and all tests pass
- MenuScreen tested for: title display, New Game button, Best Scores button, conditional Resume (shown when true, absent when false), all 3 callbacks
- DifficultyScreen tested for: heading display, all 3 difficulty buttons, Back button, correct Difficulty enum passed in callbacks
- SummaryScreen and LeaderboardScreen tests updated to use onBackToMenu and "Back to Menu" label
- Full test suite (`testDebugUnitTest`) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/06-menu-navigation/06-02-SUMMARY.md`
</output>
