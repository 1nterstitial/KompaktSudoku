---
phase: quick
plan: 260327-0yw
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - app/src/test/AndroidManifest.xml
  - app/src/main/java/com/ledgerman/sudoku/**/*.kt
  - app/src/test/java/com/ledgerman/sudoku/**/*.kt
  - app/src/main/java/com/mudita/sudoku/ (deleted)
  - app/src/test/java/com/mudita/sudoku/ (deleted)
autonomous: true
requirements: []
must_haves:
  truths:
    - "All Kotlin files use package com.ledgerman.sudoku (no com.mudita.sudoku references remain in .kt files)"
    - "build.gradle.kts namespace and applicationId are com.ledgerman.sudoku"
    - "Exit dialog shows 'Quit Game' button text instead of 'Forfeit'"
    - "Project compiles and all tests pass"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "Updated namespace and applicationId"
      contains: "com.ledgerman.sudoku"
    - path: "app/src/main/java/com/ledgerman/sudoku/ui/game/GameScreen.kt"
      provides: "Quit Game button text"
      contains: "Quit Game"
  key_links:
    - from: "all .kt files"
      to: "com.ledgerman.sudoku"
      via: "package and import declarations"
      pattern: "com\\.ledgerman\\.sudoku"
---

<objective>
Rename the package from com.mudita.sudoku to com.ledgerman.sudoku across the entire codebase, and rename "Forfeit" to "Quit Game" in the exit confirmation dialog.

Purpose: Update branding/ownership identifiers and improve user-facing label clarity.
Output: All source files under new package, updated build config, "Quit Game" label in exit dialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/build.gradle.kts
@app/src/main/java/com/mudita/sudoku/ui/game/GameScreen.kt
@app/src/main/java/com/mudita/sudoku/game/GameViewModel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package from com.mudita.sudoku to com.ledgerman.sudoku</name>
  <files>
    app/build.gradle.kts,
    app/src/test/AndroidManifest.xml,
    All .kt files under app/src/main/java/ and app/src/test/java/
  </files>
  <action>
    1. Create new directory structure:
       - app/src/main/java/com/ledgerman/sudoku/ (with game/, game/model/, puzzle/, puzzle/engine/, puzzle/model/, ui/, ui/game/ subdirs)
       - app/src/test/java/com/ledgerman/sudoku/ (with game/, puzzle/, ui/game/ subdirs)

    2. Move all .kt files from com/mudita/sudoku/** to com/ledgerman/sudoku/** preserving relative paths.

    3. In EVERY .kt file, replace all occurrences of "com.mudita.sudoku" with "com.ledgerman.sudoku" in:
       - `package` declarations
       - `import` statements

    4. In app/build.gradle.kts:
       - Change `namespace = "com.mudita.sudoku"` to `namespace = "com.ledgerman.sudoku"`
       - Change `applicationId = "com.mudita.sudoku"` to `applicationId = "com.ledgerman.sudoku"`

    5. In app/src/test/AndroidManifest.xml: replace com.mudita.sudoku with com.ledgerman.sudoku if present.

    6. Delete the old empty directory tree: app/src/main/java/com/mudita/sudoku/ and app/src/test/java/com/mudita/sudoku/

    IMPORTANT: Do NOT touch files under app/src/main/java/com/mudita/mmd/ — those are MMD stubs with their own package and must remain as-is.
  </action>
  <verify>
    <automated>cd D:/Development/KompaktSudoku && grep -r "com\.mudita\.sudoku" app/src --include="*.kt" --include="*.kts" --include="*.xml" | grep -v "com\.mudita\.mmd" | head -5; echo "Exit: $?"</automated>
  </verify>
  <done>Zero grep matches for com.mudita.sudoku in .kt/.kts/.xml files (excluding com.mudita.mmd stubs). Old directories deleted. New directory structure at com/ledgerman/sudoku.</done>
</task>

<task type="auto">
  <name>Task 2: Rename "Forfeit" to "Quit Game" in exit dialog</name>
  <files>
    app/src/main/java/com/ledgerman/sudoku/ui/game/GameScreen.kt,
    app/src/main/java/com/ledgerman/sudoku/game/GameViewModel.kt
  </files>
  <action>
    1. In GameScreen.kt (ExitConfirmationDialog composable):
       - Rename the `onForfeit` parameter to `onQuitGame`
       - Change the button text from "Forfeit" to "Quit Game"
       - Update the KDoc comment: replace "Forfeit" with "Quit Game"

    2. In GameScreen.kt (GameScreen composable) where ExitConfirmationDialog is called:
       - Update the named argument from `onForfeit =` to `onQuitGame =`

    3. In GameViewModel.kt:
       - Update the KDoc comment on quitGame() function: replace "Forfeit" with "Quit Game" and "forfeiting" with "quitting"
       - Do NOT rename the quitGame() function itself — it already has the correct name

    Note: The function is already named quitGame() — only UI text and comments reference "Forfeit".
  </action>
  <verify>
    <automated>cd D:/Development/KompaktSudoku && grep -rn -i "forfeit" app/src --include="*.kt" | head -5; echo "Exit: $?"</automated>
  </verify>
  <done>No remaining references to "Forfeit" in any .kt file. Exit dialog button reads "Quit Game". Parameter named onQuitGame.</done>
</task>

</tasks>

<verification>
1. `grep -r "com\.mudita\.sudoku" app/src --include="*.kt" --include="*.kts" --include="*.xml" | grep -v mmd` returns nothing
2. `grep -ri "forfeit" app/src --include="*.kt"` returns nothing
3. `cd D:/Development/KompaktSudoku && ./gradlew assembleDebug` compiles successfully
4. `cd D:/Development/KompaktSudoku && ./gradlew test` all tests pass
</verification>

<success_criteria>
- Package is com.ledgerman.sudoku everywhere (build config, all Kotlin files)
- No remaining "Forfeit" text in any Kotlin source file
- Project compiles and all unit tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260327-0yw-two-minor-changes-1-rename-forfeit-to-qu/260327-0yw-SUMMARY.md`
</output>
