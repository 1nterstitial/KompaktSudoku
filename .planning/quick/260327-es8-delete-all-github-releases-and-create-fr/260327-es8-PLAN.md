---
phase: quick
plan: 260327-es8
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
must_haves:
  truths:
    - "All previous GitHub releases and tags are deleted"
    - "A single fresh v1.0 release exists with the existing APK attached"
    - "No personal names, private emails, or ownership identifiers appear in release metadata"
  artifacts: []
  key_links: []
---

<objective>
Delete all existing GitHub releases and tags, then create a clean fresh release with the already-built APK.

Purpose: Start with a clean release history free of any private or ownership information.
Output: A single GitHub release (v1.0) with app-release.apk attached.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Current state:
- One existing release: v1.0 at https://github.com/1nterstitial/KompaktSudoku/releases/tag/v1.0
- One existing tag: v1.0
- APK location: app/build/outputs/apk/release/app-release.apk (already built, do NOT rebuild)
- Git author is already anonymized: "GSD <dev@mudita-sudoku.local>"
- GitHub username: 1nterstitial
- Existing release description: "Fully playable Sudoku for Mudita Kompakt — 3 difficulty levels, pause/resume, leaderboard."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Privacy audit and delete all existing releases and tags</name>
  <files></files>
  <action>
1. Audit the existing release for private information:
   - Check release title, body, tag name via `gh release view v1.0`
   - Check git author/committer on tagged commit: `git log v1.0 -1 --format="%an <%ae> %cn <%ce>"`
   - Check APK metadata: `aapt dump badging app/build/outputs/apk/release/app-release.apk 2>/dev/null | grep -i -E "name|label|package"` (if aapt available, otherwise skip — APK package is com.ledgerman.sudoku which is fine as a public package name)
   - Look for real names or personal emails (anything other than "GSD", "dev@mudita-sudoku.local", "1nterstitial", "noreply@anthropic.com")
   - If any private info found, note it for exclusion from the new release

2. Delete all existing GitHub releases:
   - `gh release list --json tagName -q ".[].tagName"` to get all release tag names
   - For each: `gh release delete {tag} --yes --cleanup-tag`

3. Delete any remaining local and remote tags:
   - `git tag -l` to list local tags
   - For each: `git tag -d {tag}` (local) and `git push origin :refs/tags/{tag}` (remote, if not already cleaned by --cleanup-tag)

4. Verify clean state:
   - `gh release list` should return empty
   - `git tag -l` should return empty
   - `git ls-remote --tags origin` should return empty
  </action>
  <verify>
    <automated>gh release list 2>&1 | grep -c "v" | xargs test 0 -eq</automated>
  </verify>
  <done>All GitHub releases and git tags (local and remote) are deleted. Privacy audit completed with no issues found or issues documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create fresh v1.0 release with existing APK</name>
  <files></files>
  <action>
1. Create a new lightweight tag v1.0 on the current HEAD commit:
   - `git tag v1.0`
   - `git push origin v1.0`

2. Create the GitHub release with a clean, privacy-safe description:
   - Use `gh release create v1.0` with:
     - Title: "v1.0"
     - Body: A concise description of the app. Include only:
       - What the app is (Sudoku for Mudita Kompakt)
       - Key features (3 difficulty levels, pause/resume, best scores)
       - Installation note (sideload APK via ADB)
     - Do NOT include any personal names, emails, or internal identifiers
     - Do NOT mention Claude, AI, or any development tooling
   - Attach the existing APK: `app/build/outputs/apk/release/app-release.apk`

3. Verify the release:
   - `gh release view v1.0` to confirm title, body, and asset are correct
   - Visually confirm no private information in the output
  </action>
  <verify>
    <automated>gh release view v1.0 --json tagName,assets -q '"\(.tagName) assets:\(.assets | length)"' | grep "v1.0 assets:1"</automated>
  </verify>
  <done>Fresh v1.0 release exists on GitHub with app-release.apk attached. Release description contains no private or ownership information.</done>
</task>

</tasks>

<verification>
- `gh release list` shows exactly one release: v1.0
- `gh release view v1.0` shows clean description with APK asset
- `git tag -l` shows only v1.0
- No real names, personal emails, or private identifiers in release metadata
</verification>

<success_criteria>
- All old releases and tags deleted
- Single v1.0 release exists with app-release.apk attached
- Release metadata is free of private/ownership information
</success_criteria>

<output>
After completion, create `.planning/quick/260327-es8-delete-all-github-releases-and-create-fr/260327-es8-SUMMARY.md`
</output>
