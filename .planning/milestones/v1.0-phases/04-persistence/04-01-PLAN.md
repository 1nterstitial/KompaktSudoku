---
phase: 04-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/mudita/sudoku/game/model/PersistedGameState.kt
  - app/src/main/java/com/mudita/sudoku/game/GameRepository.kt
  - app/src/main/java/com/mudita/sudoku/game/DataStoreGameRepository.kt
  - app/src/test/java/com/mudita/sudoku/game/FakeGameRepository.kt
  - app/src/test/java/com/mudita/sudoku/game/PersistedGameStateTest.kt
autonomous: true
requirements:
  - STATE-01

must_haves:
  truths:
    - "GameUiState can be converted to PersistedGameState and back without data loss"
    - "GameRepository interface defines save, load, and clear operations"
    - "DataStoreGameRepository reads and writes JSON to DataStore Preferences"
    - "FakeGameRepository allows in-memory save/load for testing"
  artifacts:
    - path: "app/src/main/java/com/mudita/sudoku/game/model/PersistedGameState.kt"
      provides: "Serialization DTO with List-based fields"
      contains: "@Serializable"
    - path: "app/src/main/java/com/mudita/sudoku/game/GameRepository.kt"
      provides: "Repository interface + NoOpGameRepository"
      exports: ["GameRepository", "NoOpGameRepository"]
    - path: "app/src/main/java/com/mudita/sudoku/game/DataStoreGameRepository.kt"
      provides: "DataStore implementation + gameDataStore extension"
      exports: ["DataStoreGameRepository", "gameDataStore"]
    - path: "app/src/test/java/com/mudita/sudoku/game/FakeGameRepository.kt"
      provides: "In-memory test double"
      exports: ["FakeGameRepository"]
    - path: "app/src/test/java/com/mudita/sudoku/game/PersistedGameStateTest.kt"
      provides: "Round-trip serialization tests"
      contains: "class PersistedGameStateTest"
  key_links:
    - from: "PersistedGameState.kt"
      to: "GameUiState.kt"
      via: "toPersistedState() and toUiState() extension functions"
      pattern: "fun GameUiState\\.toPersistedState|fun PersistedGameState\\.toUiState"
    - from: "DataStoreGameRepository.kt"
      to: "PersistedGameState.kt"
      via: "Json.encodeToString / decodeFromString"
      pattern: "Json\\.encodeToString|Json\\.decodeFromString"
---

<objective>
Create the persistence data layer: a serialization DTO that maps GameUiState to JSON-safe types, a repository interface for save/load/clear, the DataStore implementation, a fake for testing, and round-trip serialization tests.

Purpose: Establish the data contracts and persistence infrastructure that Plan 02 (ViewModel integration) and Plan 03 (UI wiring) depend on.
Output: PersistedGameState.kt, GameRepository.kt, DataStoreGameRepository.kt, FakeGameRepository.kt, PersistedGameStateTest.kt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persistence/04-CONTEXT.md
@.planning/phases/04-persistence/04-RESEARCH.md
@.planning/phases/04-persistence/04-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/src/main/java/com/mudita/sudoku/game/model/GameUiState.kt:
```kotlin
data class GameUiState(
    val board: IntArray = IntArray(81),
    val solution: IntArray = IntArray(81),
    val givenMask: BooleanArray = BooleanArray(81),
    val difficulty: Difficulty = Difficulty.EASY,
    val selectedCellIndex: Int? = null,
    val inputMode: InputMode = InputMode.FILL,
    val pencilMarks: Array<Set<Int>> = Array(81) { emptySet() },
    val errorCount: Int = 0,
    val isComplete: Boolean = false,
    val isLoading: Boolean = false
)
```

From app/src/main/java/com/mudita/sudoku/puzzle/model/Difficulty.kt:
```kotlin
enum class Difficulty { EASY, MEDIUM, HARD }
```

From app/src/test/java/com/mudita/sudoku/game/FakeGenerator.kt:
```kotlin
class FakeGenerator {
    companion object {
        val SOLUTION: IntArray   // 81-element valid solution
        val BOARD: IntArray      // 61 givens, 20 empty cells
        fun emptyIndices(): List<Int>
        fun correctDigitAt(index: Int): Int
        fun wrongDigitAt(index: Int): Int
    }
    fun generatePuzzle(difficulty: Difficulty): SudokuPuzzle
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersistedGameState DTO + serialization round-trip tests</name>
  <files>
    app/src/main/java/com/mudita/sudoku/game/model/PersistedGameState.kt,
    app/src/test/java/com/mudita/sudoku/game/PersistedGameStateTest.kt
  </files>
  <read_first>
    app/src/main/java/com/mudita/sudoku/game/model/GameUiState.kt,
    app/src/main/java/com/mudita/sudoku/puzzle/model/Difficulty.kt,
    app/src/main/java/com/mudita/sudoku/game/model/InputMode.kt,
    app/src/test/java/com/mudita/sudoku/game/FakeGenerator.kt,
    app/build.gradle.kts
  </read_first>
  <behavior>
    - Test 1: Round-trip conversion — create a GameUiState with non-default values for all fields, convert to PersistedGameState via toPersistedState(), convert back via toUiState(), assert all persisted fields are equal (board, solution, givenMask, difficulty, selectedCellIndex, pencilMarks, errorCount, isComplete)
    - Test 2: inputMode is NOT persisted — after round-trip, inputMode defaults to FILL regardless of original
    - Test 3: isLoading is NOT persisted — after round-trip, isLoading defaults to false regardless of original
    - Test 4: JSON round-trip — encodeToString then decodeFromString on PersistedGameState produces identical object
    - Test 5: Pencil marks with non-empty sets — GameUiState with pencilMarks containing sets like {1,3,7} round-trips correctly, sorted as [1,3,7] in JSON
    - Test 6: Null selectedCellIndex — round-trips correctly (default null)
    - Test 7: Empty board (all zeros) — round-trips correctly
  </behavior>
  <action>
    **RED phase — write tests first in PersistedGameStateTest.kt:**

    Create `app/src/test/java/com/mudita/sudoku/game/PersistedGameStateTest.kt`:
    - Package: `com.mudita.sudoku.game`
    - Import: `com.mudita.sudoku.game.model.*`, `com.mudita.sudoku.puzzle.model.Difficulty`, `kotlinx.serialization.json.Json`, `kotlinx.serialization.encodeToString`, `kotlinx.serialization.decodeFromString`, `org.junit.Assert.*`, `org.junit.Test`
    - Use FakeGenerator.BOARD and FakeGenerator.SOLUTION for realistic test data
    - Build a GameUiState with: board=FakeGenerator.BOARD.copyOf(), solution=FakeGenerator.SOLUTION.copyOf(), givenMask=BooleanArray(81){i -> FakeGenerator.BOARD[i]!=0}, difficulty=Difficulty.MEDIUM, selectedCellIndex=5, inputMode=InputMode.PENCIL, pencilMarks=Array(81){if(it==0) setOf(1,3,7) else emptySet()}, errorCount=3, isComplete=false, isLoading=true
    - Test `toPersistedState()` then `toUiState()` round-trip preserves all persisted fields
    - Test that `inputMode` resets to FILL and `isLoading` resets to false after round-trip
    - Test JSON round-trip: `Json.decodeFromString<PersistedGameState>(Json.encodeToString(persisted))` equals original

    **GREEN phase — implement PersistedGameState.kt:**

    Create `app/src/main/java/com/mudita/sudoku/game/model/PersistedGameState.kt`:
    - Package: `com.mudita.sudoku.game.model`
    - `@Serializable data class PersistedGameState` with fields:
      - `val board: List<Int>` (81 elements)
      - `val solution: List<Int>` (81 elements)
      - `val givenMask: List<Boolean>` (81 elements)
      - `val difficulty: String` (Difficulty enum name, e.g. "EASY")
      - `val selectedCellIndex: Int? = null`
      - `val pencilMarks: List<List<Int>>` (81 elements, each a sorted list of candidate digits)
      - `val errorCount: Int`
      - `val isComplete: Boolean`
    - Extension function `fun GameUiState.toPersistedState(): PersistedGameState` that maps:
      - `board.toList()`, `solution.toList()`, `givenMask.toList()`
      - `difficulty.name` for difficulty
      - `selectedCellIndex` as-is
      - `pencilMarks.map { it.sorted() }` for deterministic JSON
      - `errorCount` as-is, `isComplete` as-is
    - Extension function `fun PersistedGameState.toUiState(): GameUiState` that maps:
      - `board.toIntArray()`, `solution.toIntArray()`, `givenMask.toBooleanArray()`
      - `Difficulty.valueOf(difficulty)` for difficulty
      - `selectedCellIndex` as-is
      - `Array(81) { i -> pencilMarks[i].toSet() }` for pencilMarks
      - `errorCount` as-is, `isComplete` as-is
      - `inputMode` defaults to `InputMode.FILL` (intentional on resume)
      - `isLoading` defaults to `false` (correct)
    - Import: `com.mudita.sudoku.puzzle.model.Difficulty`, `kotlinx.serialization.Serializable`
  </action>
  <verify>
    <automated>cd /d/Development/KompaktSudoku && ./gradlew :app:testDebugUnitTest --tests "*PersistedGameStateTest*" -x lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/main/java/com/mudita/sudoku/game/model/PersistedGameState.kt` exists and contains `@Serializable`
    - File contains `data class PersistedGameState(`
    - File contains `fun GameUiState.toPersistedState(): PersistedGameState`
    - File contains `fun PersistedGameState.toUiState(): GameUiState`
    - File contains `val board: List<Int>`
    - File contains `val pencilMarks: List<List<Int>>`
    - File contains `Difficulty.valueOf(difficulty)`
    - File `app/src/test/java/com/mudita/sudoku/game/PersistedGameStateTest.kt` exists and contains `class PersistedGameStateTest`
    - `./gradlew :app:testDebugUnitTest --tests "*PersistedGameStateTest*" -x lint` exits with code 0
  </acceptance_criteria>
  <done>PersistedGameState DTO serializes and deserializes all game state fields; inputMode and isLoading are intentionally excluded from persistence; all 7 test behaviors pass</done>
</task>

<task type="auto">
  <name>Task 2: GameRepository interface + DataStoreGameRepository + FakeGameRepository</name>
  <files>
    app/src/main/java/com/mudita/sudoku/game/GameRepository.kt,
    app/src/main/java/com/mudita/sudoku/game/DataStoreGameRepository.kt,
    app/src/test/java/com/mudita/sudoku/game/FakeGameRepository.kt
  </files>
  <read_first>
    app/src/main/java/com/mudita/sudoku/game/model/PersistedGameState.kt,
    app/src/main/java/com/mudita/sudoku/game/model/GameUiState.kt,
    app/build.gradle.kts
  </read_first>
  <action>
    **Create `app/src/main/java/com/mudita/sudoku/game/GameRepository.kt`:**
    - Package: `com.mudita.sudoku.game`
    - Import: `com.mudita.sudoku.game.model.GameUiState`
    - Define interface:
      ```kotlin
      interface GameRepository {
          suspend fun saveGame(state: GameUiState)
          suspend fun loadGame(): GameUiState?
          suspend fun clearGame()
      }
      ```
    - Define `NoOpGameRepository` in same file:
      ```kotlin
      class NoOpGameRepository : GameRepository {
          override suspend fun saveGame(state: GameUiState) {}
          override suspend fun loadGame(): GameUiState? = null
          override suspend fun clearGame() {}
      }
      ```

    **Create `app/src/main/java/com/mudita/sudoku/game/DataStoreGameRepository.kt`:**
    - Package: `com.mudita.sudoku.game`
    - At FILE SCOPE (top level, before class): `val Context.gameDataStore: DataStore<Preferences> by preferencesDataStore(name = "game_state")`
    - Private constant: `private val IN_PROGRESS_GAME_KEY = stringPreferencesKey("in_progress_game")`
    - Class `DataStoreGameRepository(private val dataStore: DataStore<Preferences>) : GameRepository`:
      - `saveGame`: `withContext(Dispatchers.IO)` block, `Json.encodeToString(state.toPersistedState())`, `dataStore.edit { prefs -> prefs[IN_PROGRESS_GAME_KEY] = json }`
      - `loadGame`: `withContext(Dispatchers.IO)` block, `dataStore.data.first()`, null-check the key, `Json.decodeFromString<PersistedGameState>(json).toUiState()`, catch `Exception` returning `null` (corrupt data = no saved game)
      - `clearGame`: `withContext(Dispatchers.IO)` block, `dataStore.edit { prefs -> prefs.remove(IN_PROGRESS_GAME_KEY) }`
    - Imports: `android.content.Context`, `androidx.datastore.core.DataStore`, `androidx.datastore.preferences.core.*`, `androidx.datastore.preferences.preferencesDataStore`, `com.mudita.sudoku.game.model.*`, `kotlinx.coroutines.Dispatchers`, `kotlinx.coroutines.flow.first`, `kotlinx.coroutines.withContext`, `kotlinx.serialization.json.Json`, `kotlinx.serialization.encodeToString`, `kotlinx.serialization.decodeFromString`

    **Create `app/src/test/java/com/mudita/sudoku/game/FakeGameRepository.kt`:**
    - Package: `com.mudita.sudoku.game`
    - Import: `com.mudita.sudoku.game.model.GameUiState`
    - Class:
      ```kotlin
      class FakeGameRepository(
          private var savedState: GameUiState? = null
      ) : GameRepository {
          var saveCallCount = 0
              private set
          var clearCallCount = 0
              private set

          override suspend fun saveGame(state: GameUiState) {
              savedState = state
              saveCallCount++
          }
          override suspend fun loadGame(): GameUiState? = savedState
          override suspend fun clearGame() {
              savedState = null
              clearCallCount++
          }
      }
      ```
  </action>
  <verify>
    <automated>cd /d/Development/KompaktSudoku && ./gradlew :app:testDebugUnitTest --tests "*PersistedGameStateTest*" -x lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/main/java/com/mudita/sudoku/game/GameRepository.kt` contains `interface GameRepository`
    - File contains `suspend fun saveGame(state: GameUiState)`
    - File contains `suspend fun loadGame(): GameUiState?`
    - File contains `suspend fun clearGame()`
    - File contains `class NoOpGameRepository : GameRepository`
    - File `app/src/main/java/com/mudita/sudoku/game/DataStoreGameRepository.kt` contains `val Context.gameDataStore`
    - File contains `preferencesDataStore(name = "game_state")`
    - File contains `stringPreferencesKey("in_progress_game")`
    - File contains `class DataStoreGameRepository`
    - File contains `Json.encodeToString`
    - File `app/src/test/java/com/mudita/sudoku/game/FakeGameRepository.kt` contains `class FakeGameRepository`
    - File contains `var saveCallCount`
    - File contains `var clearCallCount`
    - Existing tests still pass: `./gradlew :app:testDebugUnitTest --tests "*PersistedGameStateTest*" -x lint` exits 0
  </acceptance_criteria>
  <done>GameRepository interface defines the save/load/clear contract; DataStoreGameRepository implements it with DataStore Preferences + JSON serialization; NoOpGameRepository provides backward-compatible default; FakeGameRepository provides in-memory test double with call counting</done>
</task>

</tasks>

<verification>
- All PersistedGameStateTest tests pass: `./gradlew :app:testDebugUnitTest --tests "*PersistedGameStateTest*" -x lint`
- No existing tests broken: `./gradlew :app:testDebugUnitTest -x lint`
- PersistedGameState.kt contains `@Serializable` annotation
- GameRepository.kt exports `GameRepository` and `NoOpGameRepository`
- DataStoreGameRepository.kt has `gameDataStore` at file scope and uses `in_progress_game` key
</verification>

<success_criteria>
- PersistedGameState round-trip preserves all game state fields (board, solution, givenMask, difficulty, selectedCellIndex, pencilMarks, errorCount, isComplete)
- inputMode and isLoading are intentionally NOT persisted
- GameRepository interface is injectable into GameViewModel
- DataStoreGameRepository uses Dispatchers.IO for all DataStore operations
- FakeGameRepository tracks save/clear call counts for test assertions
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistence/04-01-SUMMARY.md`
</output>
